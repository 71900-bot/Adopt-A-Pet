[build]
  command = "pip install -r requirements.txt"
  publish = "static" # This points to your CSS and Images

[functions]
  external_node_modules = ["flask"]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
